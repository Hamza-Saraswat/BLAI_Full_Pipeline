{
  "name": "hyperframes",
  "private": true,
  "type": "module",
  "devDependencies": {
    "hyperframes": "0.7.31"
  },
  "scripts": {
    "dev": "./node_modules/.bin/hyperframes preview",
    "check": "./node_modules/.bin/hyperframes lint && ./node_modules/.bin/hyperframes validate && ./node_modules/.bin/hyperframes inspect --samples 40 --at-transitions --strict",
    "render": "./node_modules/.bin/hyperframes render",
    "publish": "./node_modules/.bin/hyperframes publish"
  }
}
