Twenty-seven billion parameters, in a file of six point one nine gigabytes. That is the smallest published build of one Qwen model, sitting on Unsloth's file table. You own one graphics card. You want to know what actually runs on it.

Start with the honest part. Nothing in this episode was measured by us. The DGX Spark here was unreachable from the machine that makes these videos. Every number you hear carries the name of whoever published it. You can go and check each one.

The model page hands you a parameter count. That number tells you almost nothing about whether the thing will load. What decides is how many gigabytes sit resident on your card while it runs. Only one of those numbers is printed where you are looking. The other one you have to go and find.

The decision in front of you is narrower than the argument people have online. You are picking a build. One model ships many builds of itself, and their file sizes sit nowhere near each other. That gap is where tonight's decision lives.

Say you own one consumer card, and you want the whole model resident on it. No second card. No renting time on a server somewhere. Whatever your card holds tonight, that number does not move, so everything else has to.

So build one small habit. Open the files listing on the model's repository and read the size of the build you want. Unsloth publishes exactly that table for the Qwen model, in gigabytes, beside every file name. That size is where the arithmetic starts.

So is the file size the answer? Close. The file holds the weights, and it is the part you download. Three things share your card's memory once the model runs, and the file is only one of them.

Your own conversation is the cost people forget. Your model keeps running notes on every word so far. That is a KV cache, and it sits in the same memory as the weights. It starts small. It grows with every word you send and every word that comes back.

Think of the loaded weights as one room that never changes size. Your conversation keeps a desk in that room, and the desk grows all evening. Desks do not shrink, and the room does not stretch to help.

NVIDIA publishes the arithmetic for that cache. Two, times the layers, times the heads, times the head dimension, times the bytes per value. That is the cost of one token, and NVIDIA works the example through itself.

Its worked example is Llama two at seven billion parameters. At a context of four thousand and ninety-six tokens, that arithmetic lands at roughly two gigabytes of cache. On a card holding eight gigabytes of memory, two gigabytes is not a rounding error.

Now, the formula as printed counts attention heads, which was right for the models of its day. Modern ones share those notes between groups of heads. Qwen's eight billion parameter model lists thirty-two query heads against eight key-value heads.

That sharing is grouped-query attention, and it cuts the notes several times over. Run the printed formula on a model built that way and you overstate its cache by four times. Those head counts come from the config file on Qwen's own repository.

One cost is left, and nobody publishes it. A llama dot cpp maintainer says there is no easy way to calculate the context size. The same post notes the graphics runtime takes memory that may not be accounted for anywhere.

That warning turns up in Ollama's documentation too, put the other way round. Set a longer context length and the memory required goes up. So the context window you type is a purchase, and you pay for it out of this same card.

So here is the one sum you can hold in your head. File size, plus cache for the context you actually run, subtracted from the memory on your card. Whatever is left, leave some of it alone. The runtime lives there.

That sum has one dial you can turn. Quantization, storing each of the model's numbers with fewer bits, shrinks the file you download. Same model, same parameter count, smaller file. Every build on a repository page is one setting of that dial.

Unsloth's table for that twenty-seven billion parameter Qwen model starts at six point one nine gigabytes. That is its bottom rung. The top rung is twenty-five point three gigabytes. Same weights, same parameter count, and one model that reaches every consumer card tier.

That range is the whole point. An eight gigabyte card and a twenty-four gigabyte card can both run this model. They run different builds of it, and the distance between those builds is quality you agreed to give up.

You can do this sum yourself, and it will mislead you. Twenty-seven billion parameters at four bits each comes to about thirteen and a half gigabytes. That is the number a lot of people trust. The published file table does not agree with it.

The label is not the width. On llama dot cpp's own quantize table, Q four K M measures four point eight nine bits per weight. Q eight zero measures eight point five bits per weight. Each block of numbers carries a scale on top of its payload bits.

Every rung down that ladder has a published price. llama dot cpp measures it with perplexity, which is how surprised a model is by ordinary text. Lower is better. Their table runs the comparison at seven billion parameters.

At that size, Q four K M costs about plus zero point zero five perplexity. Q two K costs about plus zero point eight seven perplexity. Both of those sit on the same ladder, and they are nowhere near the same distance from the original.

So spending your budget on parameters works, down to a point. Take the largest build whose file plus cache still fits, and stop before the bottom rungs. That is the rule most people run, and for plenty of work it is fine.

Now turn that dial the other way. Take a smaller model and spend the same gigabytes on precision instead. The comparison table keeps its six columns and its units. Only the row underneath changes.

Ornith's nine billion parameter model publishes a four-bit build at five point six three gigabytes. Its eight-bit build is nine point five three gigabytes. So the entire ladder, all the way up to eight-bit, stays under ten gigabytes.

That changes the question you are asking. The top of that ladder fits a twelve gigabyte card with room to spare. You are no longer deciding whether it runs. You are deciding how much of the original model you keep. That is a better question to be stuck on.

Now the rule everyone repeats does have a source, and the source is a good one. A twenty twenty-two paper from Dettmers and Zettlemoyer concluded that four-bit precision is almost universally optimal. That is where bigger model, fewer bits comes from.

Read what the paper says it measured. Zero-shot accuracy, which means answering questions with no worked examples given first. No instruction-tuned models in the sweep. No reasoning evaluation of any kind. The paper states that scope itself.

The sizes it tested ran from nineteen million parameters up to a hundred and seventy-six billion of them. Every model in it was released before twenty twenty-two. The paper is careful about all of this. The people quoting it usually are not.

Then there is what happened to the models themselves. The Scaling Laws for Precision paper measured degradation under quantization, and it increases with training data size. That held across every model size they tested. So a model trained on more tokens has more to lose when you shrink it.

A twenty twenty-five controlled study went at reasoning directly, across roughly seventeen hundred inference scenarios. An eight billion parameter model at eight-bit consistently beat a fourteen billion parameter model at four-bit. That is the rule running backwards.

The same study goes further. A thirty-two billion parameter model at four-bit was strictly dominated by both of the others. On reasoning work, bigger and blurrier lost to smaller and sharper. Nothing about that ordering follows from the parameter count.

So when two builds both fit your card, the parameter count stops being the tiebreak. Ask what you are running the model for. If the answer is reasoning, the measured order says take the smaller model at higher precision.

Long documents pull that gap wider than reasoning does. A twenty twenty-five study went at long-context tasks. Eight-bit held accuracy there to about a zero point eight percent drop. Four-bit methods lost up to fifty-nine percent of their accuracy.

A loss of that size is not a quality nudge. That is a model going soft on the exact job you bought the memory for. Long context and cheap builds are a bad pairing.

Three situations should ignore everything so far. Start with a mixture of experts. That is a model split into specialists, with a router that wakes a few per word. Every expert stays resident in memory anyway.

Hugging Face writes it plainly. Mixtral needs enough memory for a dense forty-seven billion parameter model. It computes at the speed of a twelve billion parameter one. You pay memory for every expert and get the speed of a few.

Some models are not a fitting question at all. Unsloth's smallest published build of DeepSeek V four Flash is eighty-two and a half gigabytes. That is more than three times a twenty-four gigabyte card. No build on that page gets you there.

And your runtime may never tell you it went wrong. Ollama does not refuse a model that will not fit. It splits the layers between your card and your processor, and it keeps answering questions.

Ollama's documentation shows the shape of that. The process listing prints the split. It shows values such as forty-eight percent on the processor against fifty-two percent on the card. llama dot cpp's own guide calls that arrangement, in its words, much slower.

And nobody publishes the number you would need to do this exactly. The runtime takes its slice, and no source found for this episode puts a figure on it. So leave the top of the card alone. Do not fill it to the brim.

So here is tonight's check. Load the model you were going to run anyway, then ask Ollama for its process listing. If it prints a split, part of your model is on the processor and you are paying for it.

Take the biggest model whose file plus cache still leaves your card room to spare. If the job is reasoning or long context, spend that room on bits, not parameters.
