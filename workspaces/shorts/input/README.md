# Shorts input

Drop a note here when you want tomorrow's sweep to consider something you saw or tested (a command that surprised you, a number you measured, a thread worth a reaction). One note per item, with a URL when there is one. Stage 01 reads this folder and adds each note as a radar item tagged `source: input`. Delete the note once a Short has used it. Anything here is public (the repo is public).
