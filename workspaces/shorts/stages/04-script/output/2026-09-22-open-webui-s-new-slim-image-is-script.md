---
slug: 2026-09-22-open-webui-s-new-slim-image-is
format: classic
structure: number-first
style_pack: terminal
value_types: EQUIPS,TEACHES
promise: after about thirty-five seconds the viewer knows the slim tag is real, what it drops, and how to switch the pull tonight
target_duration_s: 38.6
brief: 2026-09-22-open-webui-s-new-slim-image-is-brief.md
drafts: 2026-09-22-open-webui-s-new-slim-image-is-drafts.md
---

# Open WebUI's new slim image is 89% smaller (working: Open WebUI Slim: An Honest 89 Percent Shrink)

## Decisions
- Structures tried: A news-react-so-what (hook named-contradiction), B number-first (hook number-shock); both cleared rotation (last two ledger shapes: contrarian-take, worked-example). B won 20 to 19 on payoff timing and voice; no grafts (judge found none that cleared the bar).
- Hook: of 10 scored, two kept, one per draft, two different patterns (finding 12). B's number-shock hook won row 1 at 3 points for both, so no hook graft.
- Advisory kept: hook_text is 8 words (cap 7) because the number-first frame-1 text must carry the whole promise; judge scored the hook row 3. Tags advisory belongs to stage 05.
- top2 soft advisory: gate wants "Docker Hub" and "amd64"; the script deliberately says "the registry itself" in narration instead of a brand call-out and never needs the architecture qualifier. entity_spend soft advisory: same reason.
- EQUIPS line: "Tonight, swap your compose file to the slim tag; your volume keeps every chat." TEACHES line: "Slim drops the bundled machine-learning stack: torch, the neural-network library, speech models, and ffmpeg, a media converter."
- Checkpoint decisions (unattended): structures+promise confirmed per hub Decisions; hooks picked as marked below.

## Hook candidates
Open WebUI just shipped itself without the ML stack. (named-contradiction) * *
Open WebUI's download just shrank eighty-nine percent. (number-shock) * *
Eighty-nine percent smaller, and the registry agrees: 168.31 MB. (number-shock)
Your chat front end just lost a gigabyte and a half of baggage. (number-shock)
Open WebUI v0.11.4 cuts its image to about 175 MB. (number-shock)
You pull Open WebUI in Docker. The pull is the problem. (situation)
The slim image is 168 megabytes. The old slim was 1.44 gigabytes. (number-shock)
Open WebUI doesn't need torch on your disk anymore. (named-contradiction)
Slim Open WebUI, honest catch: no MySQL, no S3, no local voice. (named-contradiction)
One tag suffix tonight and your next pull is seconds, not minutes. (tonight)

## Script
| Scene | Role | Narration (spoken form) | On-screen text (digits ok, 8 words max) | Visual brief | Tool | Layout | Est s |
|-------|------|-------------------------|------------------------------------------|--------------|------|--------|-------|
| s01 | hook | Open WebUI's download just shrank eighty-nine percent. You run it in Docker next to Ollama, wincing at every pull. | Open WebUI / 89% / smaller download | Dark terminal canvas, sharp corners. At frame 1 a giant hard-edged '89%' is already on screen, centered and static, with 'Open WebUI' above it. On the phrase 'just shrank', the caption 'smaller download' types in under the digits. On 'eighty-nine percent' the digits do one scale pulse, onset after 0.30s. On 'wincing at every pull' a tiny monospace compose-file snippet fades in lower-left, above the 450px safe line. Two elements animating at most. | hyperframes | giant-number | 6.6 |
| s02 | explain | Slim drops the bundled machine-learning stack: torch, the neural-network library, speech models, and ffmpeg, a media converter. Installed just in case. | torch / speech models / ffmpeg | Three sharp-cornered monospace rows type in one per beat: 'torch' lands on 'torch, the neural-network library', 'speech models' on 'speech models', 'ffmpeg' on 'ffmpeg, a media converter'. On 'Installed just in case' all three rows strike through in a single hard cut and dim to gray. Stack centered, nothing below the 450px safe line. | hyperframes | centered-stack | 7.2 |
| s03 | explain | Eighty-nine percent is against the last slim, a one point four four gigabyte pull. The standard image is still a one point five four gigabyte download. | last slim / 1.44 GB / standard image | Two hard-cornered vertical bars, bottom-aligned above the safe zone. On 'one point four four gigabyte pull' the left bar rises-in-place and its label '1.44 GB' pops in over it. On 'The standard image stays' a slightly taller bar rises to its right labeled only 'standard image' with no digits, so the spoken one point five four never appears on screen. Labels type in, motion onset after 0.30s. | manim | split-compare | 9.0 |
| s04 | explain | Slim refuses MySQL or S three storage at boot; voice and PDF reading move outside. Chat through Ollama and nothing changes. | ollama chat: same | Boot-log terminal, one hard cut per beat. On 'Slim refuses MySQL' the line 'db: mysql refused' types in. On 'or S3 storage' a second line 'storage: s3 refused' appears below it. On 'voice and PDF reading move outside' both lines hard-cut to 'voice+pdf: external'. On 'Chat through Ollama and nothing changes' the screen cuts to a single calm line, 'ollama chat: same', which holds. Never more than two short lines visible at once. | hyperframes | centered-stack | 7.2 |
| s05 | payoff_close | Tonight, swap your compose file to the slim tag; your volume keeps every chat. Your next pull is about one hundred seventy-five megabytes of download. | open-webui:slim / 175 MB / 89% | Compose-file terminal. On 'swap your compose file to the slim tag' the line types in wrapped as 'image: openwebui/' then 'open-webui:slim'. On 'your volume keeps every chat' a volume line fades in beneath, onset after 0.30s. On 'one hundred seventy-five megabytes' the terminal hard-cuts to a giant '175 MB' settling center-frame, and the frame-1 '89%' returns as a small caption under it. The video ends held on that frame, nothing after the payoff line. | hyperframes | giant-number | 8.6 |

## Notes for review
- The 89 percent is always attributed to the last release's slim (1.44 GB); the standard image is named separately at 1.54 GB. Verbatim figures live in the brief; narration rounds for the ear exactly once ("about one hundred seventy-five megabytes of download").
- "S three" is spoken as words; digits stay on screen only.
- Judge note: the number-first shape repeats the 2026-09-18 entry's pattern family (number-shock hook); difference row scored 1 for that reason and the win margin is one point. If a future ledger entry uses number-shock again, avoid number-first that day.
